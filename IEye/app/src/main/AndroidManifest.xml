<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.ieye">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".StudentdashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".Stud_profile"
            android:label="@string/title_activity_stud_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".InsertUserData" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ClassSelection" />
        <activity android:name=".BookSelection" />
        <activity android:name=".SelectCandidateList">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".VolunteerLogin" />
        <activity android:name=".Volunteer_profile" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MathBooks" />
        <activity android:name=".FileUpload" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Main3Activity"></activity>
    </application>

</manifest>